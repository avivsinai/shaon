# Gitleaks configuration for shaon
# https://github.com/gitleaks/gitleaks

title = "shaon gitleaks config"

[allowlist]
  description = "Global allowlist"
  paths = [
    '''tests/fixtures/.*''',
    '''PROTOCOL\.md''',
  ]
